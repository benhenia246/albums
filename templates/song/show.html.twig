{% extends 'base.html.twig' %}

{% block title %}Song{% endblock %}

{% block body %}
    <h1>Song</h1>

    <table class="table">
        <tbody>
{#            <tr>#}
{#                <th>Id</th>#}
{#                <td>{{ song.id }}</td>#}
{#            </tr>#}
            <tr>
                <th>Title</th>
                <td><a href="{{ song.songLink }}">{{ song.songTitle }}</a></td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ song.songDescription }}</td>
            </tr>
            <tr>
                <th>Album</th>
                <td><a href="{{ path('album_show',{'id': song.album.id}) }}"><h1>{{ song.album.title }}</h1> <img style="width: 300px; height: 300px;" src="{{ song.album.cover }}" ></a></td>
            </tr>
            <tr>
{#                <th>Link</th>#}
{#                <td>{{ song.songLink }}</td>#}
{#            </tr>#}
        </tbody>
    </table>
    <a href="{{ path('song_index') }}">back to list</a>

    <a href="{{ path('song_edit', {'id': song.id}) }}">edit</a>

    {{ include('song/_delete_form.html.twig') }}
{% endblock %}
